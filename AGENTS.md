# Agent notes (Oyabaun)

- When you complete a task that changes the tree, create a git commit on the active branch (clear message; one commit per coherent change is ideal unless the user asked otherwise).
- The user often only runs `launch` / `stop`. Run `oyabaunctl`, Blender, and `wasm-pack` yourself when needed; don’t dump commands for them unless they ask.
- Read `.cursor/rules/oyabaun-project.mdc` for stack and authority boundaries.
- Go relay rules: `.cursor/rules/go-relay.mdc`.
- Rust client rules: `.cursor/rules/rust-client.mdc`.
- Domain workflow and paths: `.cursor/skills/oyabaun/SKILL.md`.
